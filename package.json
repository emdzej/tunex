{
  "name": "tunex",
  "private": true,
  "version": "0.2.0",
  "description": "Web-based ECU firmware editor following TunerPro conventions.",
  "type": "module",
  "scripts": {
    "web": "pnpm --filter @tunex/web dev",
    "dev": "pnpm --filter @tunex/web dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  },
  "packageManager": "pnpm@10.33.1"
}
